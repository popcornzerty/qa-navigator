# API mode: "http" (default) talks to the Python/FastAPI QA engine.
# Set "mock" to run the UI on the Phase 1 in-memory fixtures, with no backend.
VITE_API_MODE=http

# Base URL of the QA engine.
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
